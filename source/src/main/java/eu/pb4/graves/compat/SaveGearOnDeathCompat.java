package eu.pb4.graves.compat;
/*
import eu.pb4.graves.GravesApi;
import net.minecraft.world.InteractionResult;

public class SaveGearOnDeathCompat {
    public static void register() {
        GravesApi.ADD_ITEM_EVENT.register((player, item) -> {
            var index = player.getInventory().getNonEquipmentItems().indexOf(item);

            return index > 8 ? InteractionResult.PASS : InteractionResult.FAIL;
        });
    }
}
*/